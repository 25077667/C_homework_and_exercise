#ifndef myheader
#define myheader

#include <stdio.h>
#include <stdlib.h>

void mySort(int *arr, int front, int tail);

int myBinarySearch(int *arr, int target, int head, int tail);
//if find the target will return the index, else will return -1

#endif  // myheader
